@model  Vidly.Models.ViewModels.EnvironmentViewModel
@{
    ViewBag.Title = "BIG RED SWITCH(BRS)";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>BIG RED SWITCH(BRS)</h1>
 
@*@model Vidly.Models.ViewModels.NewCustomerViewModel
    @{
        ViewBag.Title = "New";
        Layout = "~/Views/Shared/_Layout.cshtml";
    }

    <h4>New Customer</h4>*@

@using (Html.BeginForm("Save", "Customers"))

{
    @Html.ValidationSummary()
    @*<div class="form-group">
            @Html.LabelFor(m => m.Customer.Name)
            @Html.TextBoxFor(m => m.Customer.Name, new { @class = "form-control" })
            @Html.ValidationMessageFor(m => m.Customer.Name)
        </div>*@

    @*<div class="form-group">
            @Html.LabelFor(m => m.Customer.Birthdate)
            @Html.TextBoxFor(m => m.Customer.Birthdate, "{0:dd mm yyyy}", new { @class = "form-control" })
            @Html.ValidationMessageFor(m => m.Customer.Birthdate)
        </div>*@

    <div class="form-group">
        @Html.LabelFor(m => m.Environment.EnvironmentName)
        @Html.DropDownListFor(m => m.Environment.EnvironmentName, new SelectList(Model.Environments, "EnvironmentId", "EnvironmentName"), "Select An Environment Type", new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.Environment.EnvironmentId)
    </div>

        @*<div class="checkbox">
                <label>
                    @Html.CheckBoxFor(m => m.Customer.IsSubscribedToNewsLetter) Is subscribed to NewsLetter
                </label>
            </div>
            @Html.HiddenFor(m => m.Customer.Id)
            Html.AntiForgeryToken()
              <button type="submit" class="btn btn-primary">Save</button>*@
}
  
@section scripts{

    @Scripts.Render("~/bundles/jqueryval ")
}

